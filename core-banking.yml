spring:
  datasource:
    platform: sqlserver
    url: jdbc:sqlserver://localhost:1433;databaseName=core-banking-db;trustServerCertificate=true
    username: ${db-username}
    password: ${db-password}
    initialization-mode: always
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
    hibernate:
      ddl-auto: update
server:
  port: 8089
  servlet:
    context-path: /

eureka:
  instance:
    preferIpAddress: true # Register with IP address in Local environment as DNS is not available for local environment
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka


management:
  endpoints:
    web:
      exposure:
        include: "*"
  metrics:
    export:
      prometheus:
        enabled: true
  endpoint:
    shutdown:
      enabled: true
    prometheus:
      enabled: true
  info:
    env:
      enabled: true

endpoints:
  shutdown:
    enabled: true


info:
  app:
    name: "core-banking"
    description: "Prime Bank Core Banking Application"
    version: "1.0.0"