spring:
  datasource:
    platform: sqlserver
    url: jdbc:sqlserver://localhost:1433;databaseName=accounts-db;trustServerCertificate=true
    username: ${db-username}
    password: ${db-password}
    initialization-mode: always
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
    hibernate:
      ddl-auto: update
server:
  port: 8088
  servlet:
    context-path: /accounts

eureka:
  instance:
    preferIpAddress: true # Register with IP address in Local environment as DNS is not available for local environment
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka


info:
  app:
    name: "accounts"
    description: "Mini Bank Accounts Application"
    version: "1.0.0"


management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
  info:
    env:
      enabled: true

endpoints:
  shutdown:
    enabled: true

